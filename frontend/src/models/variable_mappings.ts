export interface variable_mappings {
    mapping_id: number;
    variable_id: number;  // Changed from formula_variables to number
    subgroup_tag_id: number;  // Changed from Subgroup_tag to number
    context_tag_id: number;
}